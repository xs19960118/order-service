spring:
  application:
    name: order-service
  profiles:
    active: dev

  datasource:
    master:
      url: jdbc:mysql://localhost:13307/order_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
      username: root
      password: xsailxma
      driver-class-name: com.mysql.cj.jdbc.Driver
    slaves:
      - url: jdbc:mysql://localhost:13308/order_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
        username: root
        password: xsailxma
        driver-class-name: com.mysql.cj.jdbc.Driver
      - url: jdbc:mysql://localhost:13309/order_db?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
        username: root
        password: xsailxma
        driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      cluster:
        nodes:
          - 192.168.1.63:7001
          - 192.168.1.63:7002
          - 192.168.1.63:7003
          - 192.168.1.63:7004
          - 192.168.1.63:7005
          - 192.168.1.63:7006
        max-redirects: 3
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 1
          max-wait: 2000ms

kafka:
  bootstrap-servers: localhost:9092

mybatis-plus:
  mapper-locations: classpath:mappers/*.xml
  type-aliases-package: com.xs.order.service.domain.model
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    root: INFO
    com.xs.order.service.infrastructure.persistence.mapper: DEBUG

server:
  port: 8080
# redis cluster configured
